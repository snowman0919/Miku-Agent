# Backend service

V0.7.x부터 HTTPS/WebSocket endpoint, Clerk token 검증, backend access grant, resource ownership, scheduler와 service orchestration을 담당한다. 인증된 request/event를 입력받아 정책 검증된 service response를 출력한다. 메모리와 모델 자체의 source of truth는 소유하지 않는다.

