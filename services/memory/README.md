# Memory service

V0.7.x부터 `user_id AND character_id` namespace의 canonical record, commit, retrieval, conflict와 user control을 담당한다. evidence-bearing observation과 user operation을 입력받아 versioned record와 provenance event를 출력한다.

