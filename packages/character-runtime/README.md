# Character runtime contract

V0.8.x부터 procedural physiology, FSM, behavior tree, high-level reaction command를 클라이언트별 renderer에 연결한다. character/audio semantic state를 입력받아 안정적인 표정·gesture·gaze·posture를 출력하며 임의 per-frame bone generation은 허용하지 않는다.

