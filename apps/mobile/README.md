# Mobile client

Flutter 기반 character-first 모바일 UI의 책임 경계다. V0.8.x부터 VRM 표현, 음성·touch interaction, memory/account UI와 push result 조회를 구현한다. 입력은 공통 인증·agent/audio·memory·reaction protocol이고, 출력은 사용자 intent와 rendering 상태다. V0.0.0에는 scaffold나 runtime code가 없다.

